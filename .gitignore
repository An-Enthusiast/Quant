__pycache__/
*.py[cod]
*.so
*.egg-info/
.venv/
venv/
build/
csrc/build/
*.duckdb
*.duckdb.wal
data/db/*.duckdb
data/db/*.duckdb.wal
*.log
.pytest_cache/
.mypy_cache/
.DS_Store
*.pkl
*.joblib
dist/

# Secrets -- broker credentials (SHOONYA_*, UPSTOX_*) must only ever live in
# untracked local env files or the real environment, never in git. See
# docs/WHITEPAPER.md "Safety & scope boundaries" and CLAUDE.md.
.env
.env.*
!.env.example
*.pem
*.key
secrets/
credentials.json
